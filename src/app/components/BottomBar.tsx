import { styled } from "../../../stitches.theme";

export const BottomBar = styled("div", {
    position: "absolute",
    bottom: 24,
    right: 16,
    padding: 0,
    display: "flex",
    gap: "$1",
    justifyContent: "end"
})